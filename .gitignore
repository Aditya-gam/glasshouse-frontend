node_modules/
.next/
out/
build/
coverage/
.turbo/

# next.js
next-env.d.ts
*.tsbuildinfo

# deploy / tooling
.vercel
test-results/
playwright-report/
*.pem

# local preview/dev launcher (machine-specific)
.claude/launch.json

# env — never commit
.env
.env.*
!.env.example

.DS_Store
*.log

# iCloud Drive sync conflict copies — this repo lives under ~/Documents, which
# iCloud syncs; on a conflict it writes "foo.ts" -> "foo 2.ts" (and "LICENSE 2").
* [0-9].*
* [0-9]
